'use strict';

var app = require("../src/app.js")
var assert = require("assert");
var proxyquire = require("proxyquire");
var data_driven = require("data_driven");

describe('running Robot wars', function() {
    it('should read first line', function() {
        
    });
});